<view class="loginBtnViews"> 
 <navigator url="/pages/schedule/schedule">
  <button class="loginBtn" type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}" disabled="{{disabled}}" bindtap="login">继续</button>
</navigator> 
 </view> 
